interface:
  display_name: "EdgePilot"
  short_description: "Backtest and run trading strategies"
  default_prompt: "Use $edgepilot in my language and respond directly to my request. Before onboarding, call verify_activation; when ready, open the Dashboard immediately if requested, show its verified local link, and ask the first V2 preference question in the same reply. Continue one question at a time and call recommend_strategies after confirmation so ChatGPT can render the three cards. Require a Codex restart only when verify_activation reports restart_required. Mention Dashboard lifecycle or background operation only when I ask about it."
